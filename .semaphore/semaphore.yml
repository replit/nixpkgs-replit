version: v1.0
name: test
agent:
  machine:
    type: e1-standard-4
    os_image: nix-os-21-05
fail_fast:
  stop:
    when: "true"
auto_cancel:
  running:
    when: branch != 'master'
blocks:
  - name: build
    task:
      jobs:
        - name: build custom packages
          commands:
            - nix-build shell.nix
